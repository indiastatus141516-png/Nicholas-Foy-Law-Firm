# Nicholas Foy Law Firm — React Clone

This repository contains a minimal React + Vite clone of the provided static HTML/CSS page.

Quick start (PowerShell on Windows):

```powershell
# 1. Install dependencies
npm install

# 2. Start dev server
npm run dev

# 3. Build
npm run build

# 4. Preview production build
npm run preview
```

Notes:
- The project uses Vite + React. No server-side behavior is included.
- The styles are in `src/styles.css` and the main component is `src/App.jsx`.
